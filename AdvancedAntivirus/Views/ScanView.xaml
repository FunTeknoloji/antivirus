<UserControl x:Class="AdvancedAntivirus.Views.ScanView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel>
            <TextBlock Text="Tarama Merkezi" FontSize="28" FontWeight="Bold" Foreground="White" Margin="0,0,0,20"/>

            <UniformGrid Columns="2">
                <Border Background="#313244" CornerRadius="12" Padding="20" Margin="0,0,10,10">
                    <StackPanel>
                        <TextBlock Text="Hızlı Tarama" Foreground="White" FontSize="18" FontWeight="Bold"/>
                        <TextBlock Text="Kritik sistem dosyalarını kontrol eder." Foreground="#A6ADC8" Margin="0,5,0,15"/>
                        <Button Content="Taramayı Başlat" Style="{StaticResource PrimaryButtonStyle}" Command="{Binding QuickScanCommand}"/>
                    </StackPanel>
                </Border>

                <Border Background="#313244" CornerRadius="12" Padding="20" Margin="10,0,0,10">
                    <StackPanel>
                        <TextBlock Text="Tam Tarama" Foreground="White" FontSize="18" FontWeight="Bold"/>
                        <TextBlock Text="Tüm sürücüleri tarar." Foreground="#A6ADC8" Margin="0,5,0,15"/>
                        <Button Content="Taramayı Başlat" Style="{StaticResource PrimaryButtonStyle}" IsEnabled="False"/>
                    </StackPanel>
                </Border>
            </UniformGrid>

            <TextBlock Text="Son Tespit Edilenler" Foreground="White" FontSize="18" FontWeight="Bold" Margin="0,20,0,10"/>
            <ListBox ItemsSource="{Binding DataContext.RecentThreats, RelativeSource={RelativeSource AncestorType=Window}}" Background="#181825" BorderThickness="0" Foreground="#CDD6F4">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="#313244" BorderThickness="0,0,0,1" Padding="10">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel>
                                    <TextBlock Text="{Binding FilePath}" FontWeight="Bold"/>
                                    <TextBlock Text="{Binding ThreatName}" Foreground="#F38BA8" FontSize="12"/>
                                </StackPanel>
                                <TextBlock Grid.Column="1" Text="{Binding Engine}" Foreground="#FAB387" VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </StackPanel>
    </Grid>
</UserControl>
