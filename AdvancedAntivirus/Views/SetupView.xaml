<UserControl x:Class="AdvancedAntivirus.Views.SetupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="#1E1E2E">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="400">
            <TextBlock Text="🛡️ Advanced Antivirus Kurulumu" FontSize="24" FontWeight="Bold" Foreground="White" Margin="0,0,0,20" HorizontalAlignment="Center"/>

            <TextBlock Text="Yükleme Dizini Seçin:" Foreground="#A6ADC8" Margin="0,0,0,5"/>
            <TextBox Text="{Binding InstallPath}" Background="#313244" Foreground="White" BorderThickness="0" Padding="10" Margin="0,0,0,20"/>

            <Button Content="Kurulumu Tamamla" Style="{StaticResource PrimaryButtonStyle}" Command="{Binding CompleteSetupCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>
