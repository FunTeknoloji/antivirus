<Window x:Class="AdvancedAntivirus.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AdvancedAntivirus"
        xmlns:vm="clr-namespace:AdvancedAntivirus.ViewModels"
        xmlns:views="clr-namespace:AdvancedAntivirus.Views"
        mc:Ignorable="d"
        Title="Advanced Antivirus" Height="600" Width="900"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen">

    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>

    <Window.Resources>
        <DataTemplate DataType="{x:Type vm:DashboardViewModel}">
            <views:DashboardView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:ScanViewModel}">
            <views:ScanView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:QuarantineViewModel}">
            <views:QuarantineView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:SettingsViewModel}">
            <views:SettingsView/>
        </DataTemplate>
    </Window.Resources>

    <Border CornerRadius="15" Background="#1E1E2E" BorderBrush="#313244" BorderThickness="1">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="220"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Sidebar -->
            <Border Grid.Column="0" Background="#181825" CornerRadius="15,0,0,15">
                <Grid>
                    <StackPanel Margin="20,40,20,20">
                        <TextBlock Text="🛡️ ADVANCED" Foreground="White" FontSize="20" FontWeight="Bold" Margin="0,0,0,40" HorizontalAlignment="Center"/>

                        <RadioButton Style="{StaticResource NavButtonStyle}" Content="Dashboard" IsChecked="True" Command="{Binding ShowDashboardCommand}"/>
                        <RadioButton Style="{StaticResource NavButtonStyle}" Content="Taramalar" Command="{Binding ShowScanCommand}"/>
                        <RadioButton Style="{StaticResource NavButtonStyle}" Content="Karantina" Command="{Binding ShowQuarantineCommand}"/>
                        <RadioButton Style="{StaticResource NavButtonStyle}" Content="Ayarlar" Command="{Binding ShowSettingsCommand}"/>
                    </StackPanel>

                    <StackPanel VerticalAlignment="Bottom" Margin="20">
                         <TextBlock Text="{Binding StatusText}" Foreground="#A6ADC8" HorizontalAlignment="Center" FontSize="12"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Content Area -->
            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="50"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- TitleBar / Controls -->
                <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,20,0">
                    <Button Content="—" Style="{StaticResource ControlButtonStyle}" Click="Minimize_Click"/>
                    <Button Content="✕" Style="{StaticResource ControlButtonStyle}" Click="Close_Click"/>
                </StackPanel>

                <ContentControl Grid.Row="1" Content="{Binding CurrentView}" Margin="30"/>
            </Grid>
        </Grid>
    </Border>
</Window>
